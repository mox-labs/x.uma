// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.2
//   protoc               unknown
// source: xds/type/v3/cel.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { CheckedExpr as CheckedExpr2 } from "../../../cel/expr/checked";
import { ParsedExpr as ParsedExpr1 } from "../../../cel/expr/syntax";
import { CheckedExpr } from "../../../google/api/expr/v1alpha1/checked";
import { ParsedExpr } from "../../../google/api/expr/v1alpha1/syntax";
import { StringValue } from "../../../google/protobuf/wrappers";

export const protobufPackage = "xds.type.v3";

/**
 * Either parsed or checked representation of the `Common Expression Language
 * <https://github.com/google/cel-spec>`_ (CEL) program.
 */
export interface CelExpression {
  /**
   * Parsed expression in abstract syntax tree (AST) form.
   *
   * Deprecated -- use ``cel_expr_parsed`` field instead.
   * If ``cel_expr_parsed`` or ``cel_expr_checked`` is set, this field is not used.
   *
   * @deprecated
   */
  parsedExpr?:
    | ParsedExpr
    | undefined;
  /**
   * Parsed expression in abstract syntax tree (AST) form that has been successfully type checked.
   *
   * Deprecated -- use ``cel_expr_checked`` field instead.
   * If ``cel_expr_parsed`` or ``cel_expr_checked`` is set, this field is not used.
   *
   * @deprecated
   */
  checkedExpr?:
    | CheckedExpr
    | undefined;
  /**
   * Parsed expression in abstract syntax tree (AST) form.
   *
   * If ``cel_expr_checked`` is set, this field is not used.
   */
  celExprParsed:
    | ParsedExpr1
    | undefined;
  /**
   * Parsed expression in abstract syntax tree (AST) form that has been successfully type checked.
   *
   * If set, takes precedence over ``cel_expr_parsed``.
   */
  celExprChecked:
    | CheckedExpr2
    | undefined;
  /**
   * Unparsed expression in string form. For example, ``request.headers['x-env'] == 'prod'`` will
   * get ``x-env`` header value and compare it with ``prod``.
   * Check the `Common Expression Language <https://github.com/google/cel-spec>`_ for more details.
   *
   * If set, takes precedence over ``cel_expr_parsed`` and ``cel_expr_checked``.
   */
  celExprString: string;
}

/**
 * Extracts a string by evaluating a `Common Expression Language
 * <https://github.com/google/cel-spec>`_ (CEL) expression against the standardized set of
 * :ref:`HTTP attributes <arch_overview_attributes>`.
 *
 * .. attention::
 *
 *   Besides CEL evaluation raising an error explicitly, CEL program returning a type other than
 *   the ``string``, or not returning anything, are considered an error as well.
 *
 * [#comment:TODO(sergiitk): When implemented, add the extension tag.]
 */
export interface CelExtractString {
  /**
   * The CEL expression used to extract a string from the CEL environment.
   * the "subject string") that should be replaced.
   */
  exprExtract:
    | CelExpression
    | undefined;
  /**
   * If CEL expression evaluates to an error, this value is be returned to the caller.
   * If not set, the error is propagated to the caller.
   */
  defaultValue: string | undefined;
}

function createBaseCelExpression(): CelExpression {
  return {
    parsedExpr: undefined,
    checkedExpr: undefined,
    celExprParsed: undefined,
    celExprChecked: undefined,
    celExprString: "",
  };
}

export const CelExpression: MessageFns<CelExpression> = {
  encode(message: CelExpression, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.parsedExpr !== undefined) {
      ParsedExpr.encode(message.parsedExpr, writer.uint32(10).fork()).join();
    }
    if (message.checkedExpr !== undefined) {
      CheckedExpr.encode(message.checkedExpr, writer.uint32(18).fork()).join();
    }
    if (message.celExprParsed !== undefined) {
      ParsedExpr1.encode(message.celExprParsed, writer.uint32(26).fork()).join();
    }
    if (message.celExprChecked !== undefined) {
      CheckedExpr2.encode(message.celExprChecked, writer.uint32(34).fork()).join();
    }
    if (message.celExprString !== "") {
      writer.uint32(42).string(message.celExprString);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CelExpression {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCelExpression();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.parsedExpr = ParsedExpr.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.checkedExpr = CheckedExpr.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.celExprParsed = ParsedExpr1.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.celExprChecked = CheckedExpr2.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.celExprString = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CelExpression {
    return {
      parsedExpr: isSet(object.parsedExpr)
        ? ParsedExpr.fromJSON(object.parsedExpr)
        : isSet(object.parsed_expr)
        ? ParsedExpr.fromJSON(object.parsed_expr)
        : undefined,
      checkedExpr: isSet(object.checkedExpr)
        ? CheckedExpr.fromJSON(object.checkedExpr)
        : isSet(object.checked_expr)
        ? CheckedExpr.fromJSON(object.checked_expr)
        : undefined,
      celExprParsed: isSet(object.celExprParsed)
        ? ParsedExpr1.fromJSON(object.celExprParsed)
        : isSet(object.cel_expr_parsed)
        ? ParsedExpr1.fromJSON(object.cel_expr_parsed)
        : undefined,
      celExprChecked: isSet(object.celExprChecked)
        ? CheckedExpr2.fromJSON(object.celExprChecked)
        : isSet(object.cel_expr_checked)
        ? CheckedExpr2.fromJSON(object.cel_expr_checked)
        : undefined,
      celExprString: isSet(object.celExprString)
        ? globalThis.String(object.celExprString)
        : isSet(object.cel_expr_string)
        ? globalThis.String(object.cel_expr_string)
        : "",
    };
  },

  toJSON(message: CelExpression): unknown {
    const obj: any = {};
    if (message.parsedExpr !== undefined) {
      obj.parsedExpr = ParsedExpr.toJSON(message.parsedExpr);
    }
    if (message.checkedExpr !== undefined) {
      obj.checkedExpr = CheckedExpr.toJSON(message.checkedExpr);
    }
    if (message.celExprParsed !== undefined) {
      obj.celExprParsed = ParsedExpr1.toJSON(message.celExprParsed);
    }
    if (message.celExprChecked !== undefined) {
      obj.celExprChecked = CheckedExpr2.toJSON(message.celExprChecked);
    }
    if (message.celExprString !== "") {
      obj.celExprString = message.celExprString;
    }
    return obj;
  },

  create(base?: DeepPartial<CelExpression>): CelExpression {
    return CelExpression.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CelExpression>): CelExpression {
    const message = createBaseCelExpression();
    message.parsedExpr = (object.parsedExpr !== undefined && object.parsedExpr !== null)
      ? ParsedExpr.fromPartial(object.parsedExpr)
      : undefined;
    message.checkedExpr = (object.checkedExpr !== undefined && object.checkedExpr !== null)
      ? CheckedExpr.fromPartial(object.checkedExpr)
      : undefined;
    message.celExprParsed = (object.celExprParsed !== undefined && object.celExprParsed !== null)
      ? ParsedExpr1.fromPartial(object.celExprParsed)
      : undefined;
    message.celExprChecked = (object.celExprChecked !== undefined && object.celExprChecked !== null)
      ? CheckedExpr2.fromPartial(object.celExprChecked)
      : undefined;
    message.celExprString = object.celExprString ?? "";
    return message;
  },
};

function createBaseCelExtractString(): CelExtractString {
  return { exprExtract: undefined, defaultValue: undefined };
}

export const CelExtractString: MessageFns<CelExtractString> = {
  encode(message: CelExtractString, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.exprExtract !== undefined) {
      CelExpression.encode(message.exprExtract, writer.uint32(10).fork()).join();
    }
    if (message.defaultValue !== undefined) {
      StringValue.encode({ value: message.defaultValue! }, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CelExtractString {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCelExtractString();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.exprExtract = CelExpression.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.defaultValue = StringValue.decode(reader, reader.uint32()).value;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CelExtractString {
    return {
      exprExtract: isSet(object.exprExtract)
        ? CelExpression.fromJSON(object.exprExtract)
        : isSet(object.expr_extract)
        ? CelExpression.fromJSON(object.expr_extract)
        : undefined,
      defaultValue: isSet(object.defaultValue)
        ? String(object.defaultValue)
        : isSet(object.default_value)
        ? String(object.default_value)
        : undefined,
    };
  },

  toJSON(message: CelExtractString): unknown {
    const obj: any = {};
    if (message.exprExtract !== undefined) {
      obj.exprExtract = CelExpression.toJSON(message.exprExtract);
    }
    if (message.defaultValue !== undefined) {
      obj.defaultValue = message.defaultValue;
    }
    return obj;
  },

  create(base?: DeepPartial<CelExtractString>): CelExtractString {
    return CelExtractString.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CelExtractString>): CelExtractString {
    const message = createBaseCelExtractString();
    message.exprExtract = (object.exprExtract !== undefined && object.exprExtract !== null)
      ? CelExpression.fromPartial(object.exprExtract)
      : undefined;
    message.defaultValue = object.defaultValue ?? undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
