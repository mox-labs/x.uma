// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.2
//   protoc               unknown
// source: xds/type/matcher/v3/range.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { DoubleRange, Int32Range, Int64Range } from "../../v3/range";
import { Matcher_OnMatch } from "./matcher";

export const protobufPackage = "xds.type.matcher.v3";

/**
 * Specifies a set of ranges for matching an int64 number and the associated
 * match actions.
 */
export interface Int64RangeMatcher {
  /**
   * Match a number by a list of number ranges. If multiple ranges contain the
   * input number, then the first action in this list is taken.
   */
  rangeMatchers: Int64RangeMatcher_RangeMatcher[];
}

/** Specifies a list of number ranges and a match action. */
export interface Int64RangeMatcher_RangeMatcher {
  /** A non-empty set of int64 ranges. */
  ranges: Int64Range[];
  /** Match action to apply when the input number is within one of the ranges. */
  onMatch: Matcher_OnMatch | undefined;
}

/**
 * Specifies a set of ranges for matching an int32 number and the associated
 * match actions.
 */
export interface Int32RangeMatcher {
  /**
   * Match a number by a list of number ranges. If multiple ranges contain the
   * input number, then the first action in this list is taken.
   */
  rangeMatchers: Int32RangeMatcher_RangeMatcher[];
}

/** Specifies a list of number ranges and a match action. */
export interface Int32RangeMatcher_RangeMatcher {
  /** A non-empty set of int32 ranges. */
  ranges: Int32Range[];
  /** Match action to apply when the input number is within one of the ranges. */
  onMatch: Matcher_OnMatch | undefined;
}

/**
 * Specifies a set of ranges for matching a double number and the associated
 * match actions.
 */
export interface DoubleRangeMatcher {
  /**
   * Match a number by a list of number ranges. If multiple ranges contain the
   * input number, then the first action in this list is taken.
   */
  rangeMatchers: DoubleRangeMatcher_RangeMatcher[];
}

/** Specifies a list of number ranges and a match action. */
export interface DoubleRangeMatcher_RangeMatcher {
  /** A non-empty set of double ranges. */
  ranges: DoubleRange[];
  /** Match action to apply when the input number is within one of the ranges. */
  onMatch: Matcher_OnMatch | undefined;
}

function createBaseInt64RangeMatcher(): Int64RangeMatcher {
  return { rangeMatchers: [] };
}

export const Int64RangeMatcher: MessageFns<Int64RangeMatcher> = {
  encode(message: Int64RangeMatcher, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.rangeMatchers) {
      Int64RangeMatcher_RangeMatcher.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Int64RangeMatcher {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseInt64RangeMatcher();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.rangeMatchers.push(Int64RangeMatcher_RangeMatcher.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Int64RangeMatcher {
    return {
      rangeMatchers: globalThis.Array.isArray(object?.rangeMatchers)
        ? object.rangeMatchers.map((e: any) => Int64RangeMatcher_RangeMatcher.fromJSON(e))
        : globalThis.Array.isArray(object?.range_matchers)
        ? object.range_matchers.map((e: any) => Int64RangeMatcher_RangeMatcher.fromJSON(e))
        : [],
    };
  },

  toJSON(message: Int64RangeMatcher): unknown {
    const obj: any = {};
    if (message.rangeMatchers?.length) {
      obj.rangeMatchers = message.rangeMatchers.map((e) => Int64RangeMatcher_RangeMatcher.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<Int64RangeMatcher>): Int64RangeMatcher {
    return Int64RangeMatcher.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Int64RangeMatcher>): Int64RangeMatcher {
    const message = createBaseInt64RangeMatcher();
    message.rangeMatchers = object.rangeMatchers?.map((e) => Int64RangeMatcher_RangeMatcher.fromPartial(e)) || [];
    return message;
  },
};

function createBaseInt64RangeMatcher_RangeMatcher(): Int64RangeMatcher_RangeMatcher {
  return { ranges: [], onMatch: undefined };
}

export const Int64RangeMatcher_RangeMatcher: MessageFns<Int64RangeMatcher_RangeMatcher> = {
  encode(message: Int64RangeMatcher_RangeMatcher, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.ranges) {
      Int64Range.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.onMatch !== undefined) {
      Matcher_OnMatch.encode(message.onMatch, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Int64RangeMatcher_RangeMatcher {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseInt64RangeMatcher_RangeMatcher();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.ranges.push(Int64Range.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.onMatch = Matcher_OnMatch.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Int64RangeMatcher_RangeMatcher {
    return {
      ranges: globalThis.Array.isArray(object?.ranges) ? object.ranges.map((e: any) => Int64Range.fromJSON(e)) : [],
      onMatch: isSet(object.onMatch)
        ? Matcher_OnMatch.fromJSON(object.onMatch)
        : isSet(object.on_match)
        ? Matcher_OnMatch.fromJSON(object.on_match)
        : undefined,
    };
  },

  toJSON(message: Int64RangeMatcher_RangeMatcher): unknown {
    const obj: any = {};
    if (message.ranges?.length) {
      obj.ranges = message.ranges.map((e) => Int64Range.toJSON(e));
    }
    if (message.onMatch !== undefined) {
      obj.onMatch = Matcher_OnMatch.toJSON(message.onMatch);
    }
    return obj;
  },

  create(base?: DeepPartial<Int64RangeMatcher_RangeMatcher>): Int64RangeMatcher_RangeMatcher {
    return Int64RangeMatcher_RangeMatcher.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Int64RangeMatcher_RangeMatcher>): Int64RangeMatcher_RangeMatcher {
    const message = createBaseInt64RangeMatcher_RangeMatcher();
    message.ranges = object.ranges?.map((e) => Int64Range.fromPartial(e)) || [];
    message.onMatch = (object.onMatch !== undefined && object.onMatch !== null)
      ? Matcher_OnMatch.fromPartial(object.onMatch)
      : undefined;
    return message;
  },
};

function createBaseInt32RangeMatcher(): Int32RangeMatcher {
  return { rangeMatchers: [] };
}

export const Int32RangeMatcher: MessageFns<Int32RangeMatcher> = {
  encode(message: Int32RangeMatcher, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.rangeMatchers) {
      Int32RangeMatcher_RangeMatcher.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Int32RangeMatcher {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseInt32RangeMatcher();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.rangeMatchers.push(Int32RangeMatcher_RangeMatcher.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Int32RangeMatcher {
    return {
      rangeMatchers: globalThis.Array.isArray(object?.rangeMatchers)
        ? object.rangeMatchers.map((e: any) => Int32RangeMatcher_RangeMatcher.fromJSON(e))
        : globalThis.Array.isArray(object?.range_matchers)
        ? object.range_matchers.map((e: any) => Int32RangeMatcher_RangeMatcher.fromJSON(e))
        : [],
    };
  },

  toJSON(message: Int32RangeMatcher): unknown {
    const obj: any = {};
    if (message.rangeMatchers?.length) {
      obj.rangeMatchers = message.rangeMatchers.map((e) => Int32RangeMatcher_RangeMatcher.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<Int32RangeMatcher>): Int32RangeMatcher {
    return Int32RangeMatcher.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Int32RangeMatcher>): Int32RangeMatcher {
    const message = createBaseInt32RangeMatcher();
    message.rangeMatchers = object.rangeMatchers?.map((e) => Int32RangeMatcher_RangeMatcher.fromPartial(e)) || [];
    return message;
  },
};

function createBaseInt32RangeMatcher_RangeMatcher(): Int32RangeMatcher_RangeMatcher {
  return { ranges: [], onMatch: undefined };
}

export const Int32RangeMatcher_RangeMatcher: MessageFns<Int32RangeMatcher_RangeMatcher> = {
  encode(message: Int32RangeMatcher_RangeMatcher, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.ranges) {
      Int32Range.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.onMatch !== undefined) {
      Matcher_OnMatch.encode(message.onMatch, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Int32RangeMatcher_RangeMatcher {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseInt32RangeMatcher_RangeMatcher();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.ranges.push(Int32Range.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.onMatch = Matcher_OnMatch.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Int32RangeMatcher_RangeMatcher {
    return {
      ranges: globalThis.Array.isArray(object?.ranges) ? object.ranges.map((e: any) => Int32Range.fromJSON(e)) : [],
      onMatch: isSet(object.onMatch)
        ? Matcher_OnMatch.fromJSON(object.onMatch)
        : isSet(object.on_match)
        ? Matcher_OnMatch.fromJSON(object.on_match)
        : undefined,
    };
  },

  toJSON(message: Int32RangeMatcher_RangeMatcher): unknown {
    const obj: any = {};
    if (message.ranges?.length) {
      obj.ranges = message.ranges.map((e) => Int32Range.toJSON(e));
    }
    if (message.onMatch !== undefined) {
      obj.onMatch = Matcher_OnMatch.toJSON(message.onMatch);
    }
    return obj;
  },

  create(base?: DeepPartial<Int32RangeMatcher_RangeMatcher>): Int32RangeMatcher_RangeMatcher {
    return Int32RangeMatcher_RangeMatcher.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Int32RangeMatcher_RangeMatcher>): Int32RangeMatcher_RangeMatcher {
    const message = createBaseInt32RangeMatcher_RangeMatcher();
    message.ranges = object.ranges?.map((e) => Int32Range.fromPartial(e)) || [];
    message.onMatch = (object.onMatch !== undefined && object.onMatch !== null)
      ? Matcher_OnMatch.fromPartial(object.onMatch)
      : undefined;
    return message;
  },
};

function createBaseDoubleRangeMatcher(): DoubleRangeMatcher {
  return { rangeMatchers: [] };
}

export const DoubleRangeMatcher: MessageFns<DoubleRangeMatcher> = {
  encode(message: DoubleRangeMatcher, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.rangeMatchers) {
      DoubleRangeMatcher_RangeMatcher.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DoubleRangeMatcher {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDoubleRangeMatcher();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.rangeMatchers.push(DoubleRangeMatcher_RangeMatcher.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DoubleRangeMatcher {
    return {
      rangeMatchers: globalThis.Array.isArray(object?.rangeMatchers)
        ? object.rangeMatchers.map((e: any) => DoubleRangeMatcher_RangeMatcher.fromJSON(e))
        : globalThis.Array.isArray(object?.range_matchers)
        ? object.range_matchers.map((e: any) => DoubleRangeMatcher_RangeMatcher.fromJSON(e))
        : [],
    };
  },

  toJSON(message: DoubleRangeMatcher): unknown {
    const obj: any = {};
    if (message.rangeMatchers?.length) {
      obj.rangeMatchers = message.rangeMatchers.map((e) => DoubleRangeMatcher_RangeMatcher.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<DoubleRangeMatcher>): DoubleRangeMatcher {
    return DoubleRangeMatcher.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<DoubleRangeMatcher>): DoubleRangeMatcher {
    const message = createBaseDoubleRangeMatcher();
    message.rangeMatchers = object.rangeMatchers?.map((e) => DoubleRangeMatcher_RangeMatcher.fromPartial(e)) || [];
    return message;
  },
};

function createBaseDoubleRangeMatcher_RangeMatcher(): DoubleRangeMatcher_RangeMatcher {
  return { ranges: [], onMatch: undefined };
}

export const DoubleRangeMatcher_RangeMatcher: MessageFns<DoubleRangeMatcher_RangeMatcher> = {
  encode(message: DoubleRangeMatcher_RangeMatcher, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.ranges) {
      DoubleRange.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.onMatch !== undefined) {
      Matcher_OnMatch.encode(message.onMatch, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DoubleRangeMatcher_RangeMatcher {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDoubleRangeMatcher_RangeMatcher();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.ranges.push(DoubleRange.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.onMatch = Matcher_OnMatch.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DoubleRangeMatcher_RangeMatcher {
    return {
      ranges: globalThis.Array.isArray(object?.ranges) ? object.ranges.map((e: any) => DoubleRange.fromJSON(e)) : [],
      onMatch: isSet(object.onMatch)
        ? Matcher_OnMatch.fromJSON(object.onMatch)
        : isSet(object.on_match)
        ? Matcher_OnMatch.fromJSON(object.on_match)
        : undefined,
    };
  },

  toJSON(message: DoubleRangeMatcher_RangeMatcher): unknown {
    const obj: any = {};
    if (message.ranges?.length) {
      obj.ranges = message.ranges.map((e) => DoubleRange.toJSON(e));
    }
    if (message.onMatch !== undefined) {
      obj.onMatch = Matcher_OnMatch.toJSON(message.onMatch);
    }
    return obj;
  },

  create(base?: DeepPartial<DoubleRangeMatcher_RangeMatcher>): DoubleRangeMatcher_RangeMatcher {
    return DoubleRangeMatcher_RangeMatcher.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<DoubleRangeMatcher_RangeMatcher>): DoubleRangeMatcher_RangeMatcher {
    const message = createBaseDoubleRangeMatcher_RangeMatcher();
    message.ranges = object.ranges?.map((e) => DoubleRange.fromPartial(e)) || [];
    message.onMatch = (object.onMatch !== undefined && object.onMatch !== null)
      ? Matcher_OnMatch.fromPartial(object.onMatch)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
