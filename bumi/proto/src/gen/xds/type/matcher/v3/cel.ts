// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.2
//   protoc               unknown
// source: xds/type/matcher/v3/cel.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { CelExpression } from "../../v3/cel";

export const protobufPackage = "xds.type.matcher.v3";

/**
 * Performs a match by evaluating a `Common Expression Language
 * <https://github.com/google/cel-spec>`_ (CEL) expression against the standardized set of
 * :ref:`HTTP attributes <arch_overview_attributes>` specified via ``HttpAttributesCelMatchInput``.
 *
 * .. attention::
 *
 *   The match is ``true``, iff the result of the evaluation is a bool AND true.
 *   In all other cases, the match is ``false``, including but not limited to: non-bool types,
 *   ``false``, ``null``, ``int(1)``, etc.
 *   In case CEL expression raises an error, the result of the evaluation is interpreted "no match".
 *
 * Refer to :ref:`Unified Matcher API <envoy_v3_api_msg_.xds.type.matcher.v3.Matcher>` documentation
 * for usage details.
 *
 * [#comment: envoy.matching.matchers.cel_matcher]
 */
export interface CelMatcher {
  /** Either parsed or checked representation of the CEL program. */
  exprMatch:
    | CelExpression
    | undefined;
  /**
   * Free-form description of the CEL AST, e.g. the original expression text, to be
   * used for debugging assistance.
   */
  description: string;
}

function createBaseCelMatcher(): CelMatcher {
  return { exprMatch: undefined, description: "" };
}

export const CelMatcher: MessageFns<CelMatcher> = {
  encode(message: CelMatcher, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.exprMatch !== undefined) {
      CelExpression.encode(message.exprMatch, writer.uint32(10).fork()).join();
    }
    if (message.description !== "") {
      writer.uint32(18).string(message.description);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CelMatcher {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCelMatcher();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.exprMatch = CelExpression.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.description = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CelMatcher {
    return {
      exprMatch: isSet(object.exprMatch)
        ? CelExpression.fromJSON(object.exprMatch)
        : isSet(object.expr_match)
        ? CelExpression.fromJSON(object.expr_match)
        : undefined,
      description: isSet(object.description) ? globalThis.String(object.description) : "",
    };
  },

  toJSON(message: CelMatcher): unknown {
    const obj: any = {};
    if (message.exprMatch !== undefined) {
      obj.exprMatch = CelExpression.toJSON(message.exprMatch);
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    return obj;
  },

  create(base?: DeepPartial<CelMatcher>): CelMatcher {
    return CelMatcher.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CelMatcher>): CelMatcher {
    const message = createBaseCelMatcher();
    message.exprMatch = (object.exprMatch !== undefined && object.exprMatch !== null)
      ? CelExpression.fromPartial(object.exprMatch)
      : undefined;
    message.description = object.description ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
