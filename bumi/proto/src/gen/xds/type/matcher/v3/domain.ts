// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.2
//   protoc               unknown
// source: xds/type/matcher/v3/domain.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Matcher_OnMatch } from "./matcher";

export const protobufPackage = "xds.type.matcher.v3";

/**
 * Matches a fully qualified server name against a set of domain
 * names with optional wildcards.
 */
export interface ServerNameMatcher {
  /**
   * Match a server name by multiple domain matchers. Each domain, exact or
   * wildcard, must appear at most once across all the domain matchers.
   *
   * The server name will be matched against all wildcard domains starting from
   * the longest suffix, i.e. ``www.example.com`` input will be first matched
   * against ``www.example.com``, then ``*.example.com``, then ``*.com``, then
   * ``*``, until the associated matcher action accepts the input. Note that
   * wildcards must be on a dot border, and values like ``*w.example.com`` are
   * invalid.
   */
  domainMatchers: ServerNameMatcher_DomainMatcher[];
}

/**
 * Specifies a set of exact and wildcard domains and a match action. The
 * wildcard symbol ``*`` must appear at most once as the left-most part of
 * the domain on a dot border. The wildcard matches one or more non-empty
 * domain parts.
 */
export interface ServerNameMatcher_DomainMatcher {
  /**
   * A non-empty set of domain names with optional wildcards, e.g.
   * ``www.example.com``, ``*.com``, or ``*``.
   */
  domains: string[];
  /**
   * Match action to apply when the server name matches any of the domain
   * names in the matcher.
   */
  onMatch: Matcher_OnMatch | undefined;
}

function createBaseServerNameMatcher(): ServerNameMatcher {
  return { domainMatchers: [] };
}

export const ServerNameMatcher: MessageFns<ServerNameMatcher> = {
  encode(message: ServerNameMatcher, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.domainMatchers) {
      ServerNameMatcher_DomainMatcher.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ServerNameMatcher {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseServerNameMatcher();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.domainMatchers.push(ServerNameMatcher_DomainMatcher.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ServerNameMatcher {
    return {
      domainMatchers: globalThis.Array.isArray(object?.domainMatchers)
        ? object.domainMatchers.map((e: any) => ServerNameMatcher_DomainMatcher.fromJSON(e))
        : globalThis.Array.isArray(object?.domain_matchers)
        ? object.domain_matchers.map((e: any) => ServerNameMatcher_DomainMatcher.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ServerNameMatcher): unknown {
    const obj: any = {};
    if (message.domainMatchers?.length) {
      obj.domainMatchers = message.domainMatchers.map((e) => ServerNameMatcher_DomainMatcher.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<ServerNameMatcher>): ServerNameMatcher {
    return ServerNameMatcher.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ServerNameMatcher>): ServerNameMatcher {
    const message = createBaseServerNameMatcher();
    message.domainMatchers = object.domainMatchers?.map((e) => ServerNameMatcher_DomainMatcher.fromPartial(e)) || [];
    return message;
  },
};

function createBaseServerNameMatcher_DomainMatcher(): ServerNameMatcher_DomainMatcher {
  return { domains: [], onMatch: undefined };
}

export const ServerNameMatcher_DomainMatcher: MessageFns<ServerNameMatcher_DomainMatcher> = {
  encode(message: ServerNameMatcher_DomainMatcher, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.domains) {
      writer.uint32(10).string(v!);
    }
    if (message.onMatch !== undefined) {
      Matcher_OnMatch.encode(message.onMatch, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ServerNameMatcher_DomainMatcher {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseServerNameMatcher_DomainMatcher();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.domains.push(reader.string());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.onMatch = Matcher_OnMatch.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ServerNameMatcher_DomainMatcher {
    return {
      domains: globalThis.Array.isArray(object?.domains) ? object.domains.map((e: any) => globalThis.String(e)) : [],
      onMatch: isSet(object.onMatch)
        ? Matcher_OnMatch.fromJSON(object.onMatch)
        : isSet(object.on_match)
        ? Matcher_OnMatch.fromJSON(object.on_match)
        : undefined,
    };
  },

  toJSON(message: ServerNameMatcher_DomainMatcher): unknown {
    const obj: any = {};
    if (message.domains?.length) {
      obj.domains = message.domains;
    }
    if (message.onMatch !== undefined) {
      obj.onMatch = Matcher_OnMatch.toJSON(message.onMatch);
    }
    return obj;
  },

  create(base?: DeepPartial<ServerNameMatcher_DomainMatcher>): ServerNameMatcher_DomainMatcher {
    return ServerNameMatcher_DomainMatcher.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ServerNameMatcher_DomainMatcher>): ServerNameMatcher_DomainMatcher {
    const message = createBaseServerNameMatcher_DomainMatcher();
    message.domains = object.domains?.map((e) => e) || [];
    message.onMatch = (object.onMatch !== undefined && object.onMatch !== null)
      ? Matcher_OnMatch.fromPartial(object.onMatch)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
