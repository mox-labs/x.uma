// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.2
//   protoc               unknown
// source: xds/type/matcher/v3/string.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { TypedExtensionConfig } from "../../../core/v3/extension";
import { RegexMatcher } from "./regex";

export const protobufPackage = "xds.type.matcher.v3";

/**
 * Specifies the way to match a string.
 * [#next-free-field: 9]
 */
export interface StringMatcher {
  /**
   * The input string must match exactly the string specified here.
   *
   * Examples:
   *
   * * *abc* only matches the value *abc*.
   */
  exact?:
    | string
    | undefined;
  /**
   * The input string must have the prefix specified here.
   * Note: empty prefix is not allowed, please use regex instead.
   *
   * Examples:
   *
   * * *abc* matches the value *abc.xyz*
   */
  prefix?:
    | string
    | undefined;
  /**
   * The input string must have the suffix specified here.
   * Note: empty prefix is not allowed, please use regex instead.
   *
   * Examples:
   *
   * * *abc* matches the value *xyz.abc*
   */
  suffix?:
    | string
    | undefined;
  /** The input string must match the regular expression specified here. */
  safeRegex?:
    | RegexMatcher
    | undefined;
  /**
   * The input string must have the substring specified here.
   * Note: empty contains match is not allowed, please use regex instead.
   *
   * Examples:
   *
   * * *abc* matches the value *xyz.abc.def*
   */
  contains?:
    | string
    | undefined;
  /**
   * Use an extension as the matcher type.
   * [#extension-category: envoy.string_matcher]
   */
  custom?:
    | TypedExtensionConfig
    | undefined;
  /**
   * If true, indicates the exact/prefix/suffix matching should be case insensitive. This has no
   * effect for the safe_regex match.
   * For example, the matcher *data* will match both input string *Data* and *data* if set to true.
   */
  ignoreCase: boolean;
}

/** Specifies a list of ways to match a string. */
export interface ListStringMatcher {
  patterns: StringMatcher[];
}

function createBaseStringMatcher(): StringMatcher {
  return {
    exact: undefined,
    prefix: undefined,
    suffix: undefined,
    safeRegex: undefined,
    contains: undefined,
    custom: undefined,
    ignoreCase: false,
  };
}

export const StringMatcher: MessageFns<StringMatcher> = {
  encode(message: StringMatcher, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.exact !== undefined) {
      writer.uint32(10).string(message.exact);
    }
    if (message.prefix !== undefined) {
      writer.uint32(18).string(message.prefix);
    }
    if (message.suffix !== undefined) {
      writer.uint32(26).string(message.suffix);
    }
    if (message.safeRegex !== undefined) {
      RegexMatcher.encode(message.safeRegex, writer.uint32(42).fork()).join();
    }
    if (message.contains !== undefined) {
      writer.uint32(58).string(message.contains);
    }
    if (message.custom !== undefined) {
      TypedExtensionConfig.encode(message.custom, writer.uint32(66).fork()).join();
    }
    if (message.ignoreCase !== false) {
      writer.uint32(48).bool(message.ignoreCase);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StringMatcher {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStringMatcher();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.exact = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.prefix = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.suffix = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.safeRegex = RegexMatcher.decode(reader, reader.uint32());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.contains = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.custom = TypedExtensionConfig.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.ignoreCase = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StringMatcher {
    return {
      exact: isSet(object.exact) ? globalThis.String(object.exact) : undefined,
      prefix: isSet(object.prefix) ? globalThis.String(object.prefix) : undefined,
      suffix: isSet(object.suffix) ? globalThis.String(object.suffix) : undefined,
      safeRegex: isSet(object.safeRegex)
        ? RegexMatcher.fromJSON(object.safeRegex)
        : isSet(object.safe_regex)
        ? RegexMatcher.fromJSON(object.safe_regex)
        : undefined,
      contains: isSet(object.contains) ? globalThis.String(object.contains) : undefined,
      custom: isSet(object.custom) ? TypedExtensionConfig.fromJSON(object.custom) : undefined,
      ignoreCase: isSet(object.ignoreCase)
        ? globalThis.Boolean(object.ignoreCase)
        : isSet(object.ignore_case)
        ? globalThis.Boolean(object.ignore_case)
        : false,
    };
  },

  toJSON(message: StringMatcher): unknown {
    const obj: any = {};
    if (message.exact !== undefined) {
      obj.exact = message.exact;
    }
    if (message.prefix !== undefined) {
      obj.prefix = message.prefix;
    }
    if (message.suffix !== undefined) {
      obj.suffix = message.suffix;
    }
    if (message.safeRegex !== undefined) {
      obj.safeRegex = RegexMatcher.toJSON(message.safeRegex);
    }
    if (message.contains !== undefined) {
      obj.contains = message.contains;
    }
    if (message.custom !== undefined) {
      obj.custom = TypedExtensionConfig.toJSON(message.custom);
    }
    if (message.ignoreCase !== false) {
      obj.ignoreCase = message.ignoreCase;
    }
    return obj;
  },

  create(base?: DeepPartial<StringMatcher>): StringMatcher {
    return StringMatcher.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<StringMatcher>): StringMatcher {
    const message = createBaseStringMatcher();
    message.exact = object.exact ?? undefined;
    message.prefix = object.prefix ?? undefined;
    message.suffix = object.suffix ?? undefined;
    message.safeRegex = (object.safeRegex !== undefined && object.safeRegex !== null)
      ? RegexMatcher.fromPartial(object.safeRegex)
      : undefined;
    message.contains = object.contains ?? undefined;
    message.custom = (object.custom !== undefined && object.custom !== null)
      ? TypedExtensionConfig.fromPartial(object.custom)
      : undefined;
    message.ignoreCase = object.ignoreCase ?? false;
    return message;
  },
};

function createBaseListStringMatcher(): ListStringMatcher {
  return { patterns: [] };
}

export const ListStringMatcher: MessageFns<ListStringMatcher> = {
  encode(message: ListStringMatcher, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.patterns) {
      StringMatcher.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListStringMatcher {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListStringMatcher();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.patterns.push(StringMatcher.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListStringMatcher {
    return {
      patterns: globalThis.Array.isArray(object?.patterns)
        ? object.patterns.map((e: any) => StringMatcher.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ListStringMatcher): unknown {
    const obj: any = {};
    if (message.patterns?.length) {
      obj.patterns = message.patterns.map((e) => StringMatcher.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<ListStringMatcher>): ListStringMatcher {
    return ListStringMatcher.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListStringMatcher>): ListStringMatcher {
    const message = createBaseListStringMatcher();
    message.patterns = object.patterns?.map((e) => StringMatcher.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
