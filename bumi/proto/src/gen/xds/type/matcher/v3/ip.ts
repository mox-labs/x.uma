// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.2
//   protoc               unknown
// source: xds/type/matcher/v3/ip.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { CidrRange } from "../../../core/v3/cidr";
import { Matcher_OnMatch } from "./matcher";

export const protobufPackage = "xds.type.matcher.v3";

/** Matches a specific IP address against a set of possibly overlapping subnets using a trie. */
export interface IPMatcher {
  /** Match IP address by CIDR ranges. */
  rangeMatchers: IPMatcher_IPRangeMatcher[];
}

/** Specifies a list of IP address ranges and a match action. */
export interface IPMatcher_IPRangeMatcher {
  /** A non-empty set of CIDR ranges. */
  ranges: CidrRange[];
  /** Match action to apply when the IP address is within one of the CIDR ranges. */
  onMatch:
    | Matcher_OnMatch
    | undefined;
  /**
   * Indicates whether this match option should be considered if there is a
   * more specific matcher. Exclusive matchers are not selected whenever a
   * more specific matcher exists (e.g. matcher with a longer prefix) even
   * when the more specific matcher fails its nested match condition.
   * Non-exclusive matchers are considered if the more specific matcher
   * exists but its nested match condition does not entirely match.
   * Non-exclusive matchers are selected in the order of their specificity
   * first (longest prefix first), then the order of declaration next.
   *
   * For example, consider two range matchers: an exclusive matcher *X* on
   * ``0.0.0.0/0`` and a matcher *Y* on ``192.0.0.0/2`` with a nested match
   * condition *Z*. For the input IP ``192.168.0.1`` matcher *Y* is the most
   * specific. If its nested match condition *Z* does not accept the input,
   * then the less specific matcher *X* does not apply either despite the
   * input being within the range, because matcher *X* is exclusive.
   *
   * The opposite is true if matcher *X* is not marked as exclusive. In that
   * case matcher *X* always matches whenever matcher "*Y* rejects the input.
   */
  exclusive: boolean;
}

function createBaseIPMatcher(): IPMatcher {
  return { rangeMatchers: [] };
}

export const IPMatcher: MessageFns<IPMatcher> = {
  encode(message: IPMatcher, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.rangeMatchers) {
      IPMatcher_IPRangeMatcher.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): IPMatcher {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIPMatcher();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.rangeMatchers.push(IPMatcher_IPRangeMatcher.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): IPMatcher {
    return {
      rangeMatchers: globalThis.Array.isArray(object?.rangeMatchers)
        ? object.rangeMatchers.map((e: any) => IPMatcher_IPRangeMatcher.fromJSON(e))
        : globalThis.Array.isArray(object?.range_matchers)
        ? object.range_matchers.map((e: any) => IPMatcher_IPRangeMatcher.fromJSON(e))
        : [],
    };
  },

  toJSON(message: IPMatcher): unknown {
    const obj: any = {};
    if (message.rangeMatchers?.length) {
      obj.rangeMatchers = message.rangeMatchers.map((e) => IPMatcher_IPRangeMatcher.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<IPMatcher>): IPMatcher {
    return IPMatcher.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<IPMatcher>): IPMatcher {
    const message = createBaseIPMatcher();
    message.rangeMatchers = object.rangeMatchers?.map((e) => IPMatcher_IPRangeMatcher.fromPartial(e)) || [];
    return message;
  },
};

function createBaseIPMatcher_IPRangeMatcher(): IPMatcher_IPRangeMatcher {
  return { ranges: [], onMatch: undefined, exclusive: false };
}

export const IPMatcher_IPRangeMatcher: MessageFns<IPMatcher_IPRangeMatcher> = {
  encode(message: IPMatcher_IPRangeMatcher, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.ranges) {
      CidrRange.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.onMatch !== undefined) {
      Matcher_OnMatch.encode(message.onMatch, writer.uint32(18).fork()).join();
    }
    if (message.exclusive !== false) {
      writer.uint32(24).bool(message.exclusive);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): IPMatcher_IPRangeMatcher {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIPMatcher_IPRangeMatcher();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.ranges.push(CidrRange.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.onMatch = Matcher_OnMatch.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.exclusive = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): IPMatcher_IPRangeMatcher {
    return {
      ranges: globalThis.Array.isArray(object?.ranges) ? object.ranges.map((e: any) => CidrRange.fromJSON(e)) : [],
      onMatch: isSet(object.onMatch)
        ? Matcher_OnMatch.fromJSON(object.onMatch)
        : isSet(object.on_match)
        ? Matcher_OnMatch.fromJSON(object.on_match)
        : undefined,
      exclusive: isSet(object.exclusive) ? globalThis.Boolean(object.exclusive) : false,
    };
  },

  toJSON(message: IPMatcher_IPRangeMatcher): unknown {
    const obj: any = {};
    if (message.ranges?.length) {
      obj.ranges = message.ranges.map((e) => CidrRange.toJSON(e));
    }
    if (message.onMatch !== undefined) {
      obj.onMatch = Matcher_OnMatch.toJSON(message.onMatch);
    }
    if (message.exclusive !== false) {
      obj.exclusive = message.exclusive;
    }
    return obj;
  },

  create(base?: DeepPartial<IPMatcher_IPRangeMatcher>): IPMatcher_IPRangeMatcher {
    return IPMatcher_IPRangeMatcher.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<IPMatcher_IPRangeMatcher>): IPMatcher_IPRangeMatcher {
    const message = createBaseIPMatcher_IPRangeMatcher();
    message.ranges = object.ranges?.map((e) => CidrRange.fromPartial(e)) || [];
    message.onMatch = (object.onMatch !== undefined && object.onMatch !== null)
      ? Matcher_OnMatch.fromPartial(object.onMatch)
      : undefined;
    message.exclusive = object.exclusive ?? false;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
