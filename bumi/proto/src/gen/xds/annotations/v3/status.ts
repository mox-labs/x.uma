// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.2
//   protoc               unknown
// source: xds/annotations/v3/status.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "xds.annotations.v3";

export enum PackageVersionStatus {
  /** UNKNOWN - Unknown package version status. */
  UNKNOWN = 0,
  /** FROZEN - This version of the package is frozen. */
  FROZEN = 1,
  /** ACTIVE - This version of the package is the active development version. */
  ACTIVE = 2,
  /**
   * NEXT_MAJOR_VERSION_CANDIDATE - This version of the package is the candidate for the next major version. It
   * is typically machine generated from the active development version.
   */
  NEXT_MAJOR_VERSION_CANDIDATE = 3,
  UNRECOGNIZED = -1,
}

export function packageVersionStatusFromJSON(object: any): PackageVersionStatus {
  switch (object) {
    case 0:
    case "UNKNOWN":
      return PackageVersionStatus.UNKNOWN;
    case 1:
    case "FROZEN":
      return PackageVersionStatus.FROZEN;
    case 2:
    case "ACTIVE":
      return PackageVersionStatus.ACTIVE;
    case 3:
    case "NEXT_MAJOR_VERSION_CANDIDATE":
      return PackageVersionStatus.NEXT_MAJOR_VERSION_CANDIDATE;
    case -1:
    case "UNRECOGNIZED":
    default:
      return PackageVersionStatus.UNRECOGNIZED;
  }
}

export function packageVersionStatusToJSON(object: PackageVersionStatus): string {
  switch (object) {
    case PackageVersionStatus.UNKNOWN:
      return "UNKNOWN";
    case PackageVersionStatus.FROZEN:
      return "FROZEN";
    case PackageVersionStatus.ACTIVE:
      return "ACTIVE";
    case PackageVersionStatus.NEXT_MAJOR_VERSION_CANDIDATE:
      return "NEXT_MAJOR_VERSION_CANDIDATE";
    case PackageVersionStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface FileStatusAnnotation {
  /** The entity is work-in-progress and subject to breaking changes. */
  workInProgress: boolean;
}

export interface MessageStatusAnnotation {
  /** The entity is work-in-progress and subject to breaking changes. */
  workInProgress: boolean;
}

export interface FieldStatusAnnotation {
  /** The entity is work-in-progress and subject to breaking changes. */
  workInProgress: boolean;
}

export interface StatusAnnotation {
  /** The entity is work-in-progress and subject to breaking changes. */
  workInProgress: boolean;
  /** The entity belongs to a package with the given version status. */
  packageVersionStatus: PackageVersionStatus;
}

function createBaseFileStatusAnnotation(): FileStatusAnnotation {
  return { workInProgress: false };
}

export const FileStatusAnnotation: MessageFns<FileStatusAnnotation> = {
  encode(message: FileStatusAnnotation, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.workInProgress !== false) {
      writer.uint32(8).bool(message.workInProgress);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FileStatusAnnotation {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFileStatusAnnotation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.workInProgress = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FileStatusAnnotation {
    return {
      workInProgress: isSet(object.workInProgress)
        ? globalThis.Boolean(object.workInProgress)
        : isSet(object.work_in_progress)
        ? globalThis.Boolean(object.work_in_progress)
        : false,
    };
  },

  toJSON(message: FileStatusAnnotation): unknown {
    const obj: any = {};
    if (message.workInProgress !== false) {
      obj.workInProgress = message.workInProgress;
    }
    return obj;
  },

  create(base?: DeepPartial<FileStatusAnnotation>): FileStatusAnnotation {
    return FileStatusAnnotation.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<FileStatusAnnotation>): FileStatusAnnotation {
    const message = createBaseFileStatusAnnotation();
    message.workInProgress = object.workInProgress ?? false;
    return message;
  },
};

function createBaseMessageStatusAnnotation(): MessageStatusAnnotation {
  return { workInProgress: false };
}

export const MessageStatusAnnotation: MessageFns<MessageStatusAnnotation> = {
  encode(message: MessageStatusAnnotation, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.workInProgress !== false) {
      writer.uint32(8).bool(message.workInProgress);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MessageStatusAnnotation {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMessageStatusAnnotation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.workInProgress = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MessageStatusAnnotation {
    return {
      workInProgress: isSet(object.workInProgress)
        ? globalThis.Boolean(object.workInProgress)
        : isSet(object.work_in_progress)
        ? globalThis.Boolean(object.work_in_progress)
        : false,
    };
  },

  toJSON(message: MessageStatusAnnotation): unknown {
    const obj: any = {};
    if (message.workInProgress !== false) {
      obj.workInProgress = message.workInProgress;
    }
    return obj;
  },

  create(base?: DeepPartial<MessageStatusAnnotation>): MessageStatusAnnotation {
    return MessageStatusAnnotation.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MessageStatusAnnotation>): MessageStatusAnnotation {
    const message = createBaseMessageStatusAnnotation();
    message.workInProgress = object.workInProgress ?? false;
    return message;
  },
};

function createBaseFieldStatusAnnotation(): FieldStatusAnnotation {
  return { workInProgress: false };
}

export const FieldStatusAnnotation: MessageFns<FieldStatusAnnotation> = {
  encode(message: FieldStatusAnnotation, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.workInProgress !== false) {
      writer.uint32(8).bool(message.workInProgress);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FieldStatusAnnotation {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFieldStatusAnnotation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.workInProgress = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FieldStatusAnnotation {
    return {
      workInProgress: isSet(object.workInProgress)
        ? globalThis.Boolean(object.workInProgress)
        : isSet(object.work_in_progress)
        ? globalThis.Boolean(object.work_in_progress)
        : false,
    };
  },

  toJSON(message: FieldStatusAnnotation): unknown {
    const obj: any = {};
    if (message.workInProgress !== false) {
      obj.workInProgress = message.workInProgress;
    }
    return obj;
  },

  create(base?: DeepPartial<FieldStatusAnnotation>): FieldStatusAnnotation {
    return FieldStatusAnnotation.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<FieldStatusAnnotation>): FieldStatusAnnotation {
    const message = createBaseFieldStatusAnnotation();
    message.workInProgress = object.workInProgress ?? false;
    return message;
  },
};

function createBaseStatusAnnotation(): StatusAnnotation {
  return { workInProgress: false, packageVersionStatus: 0 };
}

export const StatusAnnotation: MessageFns<StatusAnnotation> = {
  encode(message: StatusAnnotation, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.workInProgress !== false) {
      writer.uint32(8).bool(message.workInProgress);
    }
    if (message.packageVersionStatus !== 0) {
      writer.uint32(16).int32(message.packageVersionStatus);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StatusAnnotation {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStatusAnnotation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.workInProgress = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.packageVersionStatus = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StatusAnnotation {
    return {
      workInProgress: isSet(object.workInProgress)
        ? globalThis.Boolean(object.workInProgress)
        : isSet(object.work_in_progress)
        ? globalThis.Boolean(object.work_in_progress)
        : false,
      packageVersionStatus: isSet(object.packageVersionStatus)
        ? packageVersionStatusFromJSON(object.packageVersionStatus)
        : isSet(object.package_version_status)
        ? packageVersionStatusFromJSON(object.package_version_status)
        : 0,
    };
  },

  toJSON(message: StatusAnnotation): unknown {
    const obj: any = {};
    if (message.workInProgress !== false) {
      obj.workInProgress = message.workInProgress;
    }
    if (message.packageVersionStatus !== 0) {
      obj.packageVersionStatus = packageVersionStatusToJSON(message.packageVersionStatus);
    }
    return obj;
  },

  create(base?: DeepPartial<StatusAnnotation>): StatusAnnotation {
    return StatusAnnotation.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<StatusAnnotation>): StatusAnnotation {
    const message = createBaseStatusAnnotation();
    message.workInProgress = object.workInProgress ?? false;
    message.packageVersionStatus = object.packageVersionStatus ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
