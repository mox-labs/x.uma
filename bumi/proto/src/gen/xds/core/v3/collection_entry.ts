// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.2
//   protoc               unknown
// source: xds/core/v3/collection_entry.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Any } from "../../../google/protobuf/any";
import { ResourceLocator } from "./resource_locator";

export const protobufPackage = "xds.core.v3";

/**
 * xDS collection resource wrapper. This encapsulates a xDS resource when
 * appearing inside a list collection resource. List collection resources are
 * regular Resource messages of type:
 *
 * .. code-block:: proto
 *
 *   message <T>Collection {
 *     repeated CollectionEntry resources = 1;
 *   }
 */
export interface CollectionEntry {
  /** A resource locator describing how the member resource is to be located. */
  locator?:
    | ResourceLocator
    | undefined;
  /** The resource is inlined in the list collection. */
  inlineEntry?: CollectionEntry_InlineEntry | undefined;
}

/** Inlined resource entry. */
export interface CollectionEntry_InlineEntry {
  /**
   * Optional name to describe the inlined resource. Resource names must match
   * ``[a-zA-Z0-9_-\./]+`` (TODO(htuch): turn this into a PGV constraint once
   * finalized, probably should be a RFC3986 pchar). This name allows
   * reference via the #entry directive in ResourceLocator.
   */
  name: string;
  /**
   * The resource's logical version. It is illegal to have the same named xDS
   * resource name at a given version with different resource payloads.
   */
  version: string;
  /** The resource payload, including type URL. */
  resource: Any | undefined;
}

function createBaseCollectionEntry(): CollectionEntry {
  return { locator: undefined, inlineEntry: undefined };
}

export const CollectionEntry: MessageFns<CollectionEntry> = {
  encode(message: CollectionEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.locator !== undefined) {
      ResourceLocator.encode(message.locator, writer.uint32(10).fork()).join();
    }
    if (message.inlineEntry !== undefined) {
      CollectionEntry_InlineEntry.encode(message.inlineEntry, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CollectionEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCollectionEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.locator = ResourceLocator.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.inlineEntry = CollectionEntry_InlineEntry.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CollectionEntry {
    return {
      locator: isSet(object.locator) ? ResourceLocator.fromJSON(object.locator) : undefined,
      inlineEntry: isSet(object.inlineEntry)
        ? CollectionEntry_InlineEntry.fromJSON(object.inlineEntry)
        : isSet(object.inline_entry)
        ? CollectionEntry_InlineEntry.fromJSON(object.inline_entry)
        : undefined,
    };
  },

  toJSON(message: CollectionEntry): unknown {
    const obj: any = {};
    if (message.locator !== undefined) {
      obj.locator = ResourceLocator.toJSON(message.locator);
    }
    if (message.inlineEntry !== undefined) {
      obj.inlineEntry = CollectionEntry_InlineEntry.toJSON(message.inlineEntry);
    }
    return obj;
  },

  create(base?: DeepPartial<CollectionEntry>): CollectionEntry {
    return CollectionEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CollectionEntry>): CollectionEntry {
    const message = createBaseCollectionEntry();
    message.locator = (object.locator !== undefined && object.locator !== null)
      ? ResourceLocator.fromPartial(object.locator)
      : undefined;
    message.inlineEntry = (object.inlineEntry !== undefined && object.inlineEntry !== null)
      ? CollectionEntry_InlineEntry.fromPartial(object.inlineEntry)
      : undefined;
    return message;
  },
};

function createBaseCollectionEntry_InlineEntry(): CollectionEntry_InlineEntry {
  return { name: "", version: "", resource: undefined };
}

export const CollectionEntry_InlineEntry: MessageFns<CollectionEntry_InlineEntry> = {
  encode(message: CollectionEntry_InlineEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.version !== "") {
      writer.uint32(18).string(message.version);
    }
    if (message.resource !== undefined) {
      Any.encode(message.resource, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CollectionEntry_InlineEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCollectionEntry_InlineEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.version = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.resource = Any.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CollectionEntry_InlineEntry {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      version: isSet(object.version) ? globalThis.String(object.version) : "",
      resource: isSet(object.resource) ? Any.fromJSON(object.resource) : undefined,
    };
  },

  toJSON(message: CollectionEntry_InlineEntry): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.version !== "") {
      obj.version = message.version;
    }
    if (message.resource !== undefined) {
      obj.resource = Any.toJSON(message.resource);
    }
    return obj;
  },

  create(base?: DeepPartial<CollectionEntry_InlineEntry>): CollectionEntry_InlineEntry {
    return CollectionEntry_InlineEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CollectionEntry_InlineEntry>): CollectionEntry_InlineEntry {
    const message = createBaseCollectionEntry_InlineEntry();
    message.name = object.name ?? "";
    message.version = object.version ?? "";
    message.resource = (object.resource !== undefined && object.resource !== null)
      ? Any.fromPartial(object.resource)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
